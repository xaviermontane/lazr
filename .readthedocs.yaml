version: 2

build:
  os: ubuntu-24.04
  tools:
    nodejs: "20"

  commands:
    - npm ci
    - npm run docs:build
    - mkdir -p $READTHEDOCS_OUTPUT/html
    - cp -r docs/build/html/* $READTHEDOCS_OUTPUT/html